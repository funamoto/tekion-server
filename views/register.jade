extends layout

block content
  .container
    h1= title
    p.lead 登録する組織の情報を入力してください
    if (error && error.length > 0)
      h4.error-msg= error
    form(role='form', action="/management/register", method="post", style='max-width: 300px;')
      .form-group
        input.form-control(type='text', name="username", placeholder='Username')
      .form-group
        input.form-control(type='password', name="password", placeholder='Password')
      .form-group
        input.form-control(type='text', name="organization", placeholder='Organization')
      .form-group
        input.form-control(type='text', name="location", placeholder='Location')
      button.btn.btn-default(type='submit') Submit
      &nbsp;
      a(href='/')
        button.btn.btn-primary(type="button") Cancel
